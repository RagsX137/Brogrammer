FROM python:3.11-slim

RUN apt-get update -qq && apt-get install -y -qq curl nodejs npm 2>/dev/null \
    && rm -rf /var/lib/apt/lists/*

RUN pip install duckduckgo-search -q 2>/dev/null

RUN mkdir -p /workspace
WORKDIR /workspace

LABEL brogrammer.build=true
