[pytest]
pythonpath = backend .
asyncio_mode = auto
markers =
    real_llm: marks tests that require a real LLM (Ollama). Skipped by default; set RUN_REAL_LLM=1 to run.
    slow: marks tests that take >30s to run.
filterwarnings =
    ignore::pytest.PytestCollectionWarning
